syntax = "proto3";

message Message {
  string text = 1;
  string data = 2;
  string name = 3;
}

message Empty {}

service Chat {
  rpc receive_message (Empty) returns (stream Message);
  rpc send_message (Message) returns (Empty);
}